# Day 3-4：第一个 Agent

> **目标**：运行并理解第一个 Agent 示例代码
> **产出**：成功运行 FirstAgentTest.py + 代码执行流程理解

---

## 📋 今日任务清单

| 序号 | 任务 | 类型 | 预计时间 | 状态 |
|------|------|------|----------|------|
| 1 | 阅读 Agent 基本概念 | 🤖 AI辅助 | 15分钟 | ⬜ |
| 2 | 运行 FirstAgentTest.py | 🧠 自主 | 30分钟 | ⬜ |
| 3 | 理解代码执行流程 | 🤖 AI辅助 | 20分钟 | ⬜ |
| 4 | 修改问题观察变化 | 🧠 自主 | 20分钟 | ⬜ |
| 5 | 尝试添加新工具 | 🧠 自主 | 30分钟 | ⬜ |
| 6 | 记录学习笔记 | 🧠 自主 | 15分钟 | ⬜ |

**总计时间：约 2 小时**

---

## 🎯 任务详情

### 任务 1：阅读 Agent 基本概念

**AI 辅助学习** 🤖：

使用以下提示词让 AI 帮你快速理解核心概念：

```
请帮我总结 Hello-Agents 项目中关于 Agent 的核心概念，包括：
1. 什么是 Agent？用一句话定义
2. Agent 与传统 AI 程序的区别
3. Agent 的核心组成部分（用图示说明）
4. Agent 是如何工作的？简单描述执行流程

请用简洁易懂的语言，配合代码示例说明。
```

**你需要关注的内容** 🧠：

| 概念 | 核心要点 |
|------|----------|
| Agent 定义 | 能感知环境、做出决策、执行行动的智能体 |
| Agent 核心 | LLM（大脑）+ Memory（记忆）+ Tools（工具）+ Planning（规划） |
| 工作方式 | 接收任务 → 思考分析 → 调用工具 → 观察结果 → 循环或完成 |

---

### 任务 2：运行 FirstAgentTest.py

**你需要做的事情** 🧠：

```bash
# 确保在项目根目录且虚拟环境已激活
cd /Users/gaoqi/wkspace/hello-agents
source venv/bin/activate

# 进入 chapter1 目录
cd code/chapter1

# 安装额外依赖（如果需要）
pip install tavily-python openai python-dotenv requests

# 运行第一个 Agent
python FirstAgentTest.py
```

**预期输出**：

```
用户输入: 你好，请帮我查询一下今天北京的天气，然后根据天气推荐一个合适的旅游景点。
========================================
--- 循环 1 ---

正在调用大语言模型...
大语言模型响应成功。
模型输出:
Thought: 用户想要查询北京的天气，然后根据天气情况推荐合适的旅游景点...
Action: get_weather(city="北京")

🎬 行动: get_weather[北京]
👀 观察: 北京当前天气：Clear，气温15摄氏度
========================================
--- 循环 2 ---

...
任务完成，最终答案: 根据查询，北京今天天气晴朗...
```

**AI 可以帮助的事情** 🤖：

如果运行报错，使用以下提示词：

```
我在运行 FirstAgentTest.py 时遇到以下错误：
[粘贴完整错误信息]

我的环境配置：
- Python 版本：
- 已安装的依赖：[pip list 输出]
- .env 配置：[隐藏敏感信息后]

请帮我分析原因并提供解决方案。
```

---

### 任务 3：理解代码执行流程

**AI 辅助学习** 🤖：

使用以下提示词让 AI 解释代码：

```
请详细解释以下 FirstAgentTest.py 代码的执行流程：

[粘贴 FirstAgentTest.py 代码]

请按以下格式说明：
1. 整体架构：这段代码由哪几部分组成？
2. 执行流程：用流程图或步骤说明代码如何执行
3. 关键函数：每个函数的作用是什么？
4. ReAct 模式：代码中如何体现 Thought-Action-Observation？

请逐行解释关键代码，并说明为什么要这样设计。
```

**核心代码结构** 🧠：

```
FirstAgentTest.py 结构
├── 1. Agent 系统提示词（AGENT_SYSTEM_PROMPT）
│   └── 定义 Agent 的角色和行为规则
├── 2. 工具函数
│   ├── get_weather(city)  → 天气查询
│   └── get_attraction(city, weather) → 景点推荐
├── 3. LLM 客户端（OpenAICompatibleClient）
│   └── 调用大语言模型 API
├── 4. 主循环
│   ├── 构建提示词
│   ├── 调用 LLM 思考
│   ├── 解析 Action
│   ├── 执行工具
│   └── 记录 Observation
└── 5. 输出最终答案
```

**ReAct 循环示意** 🧠：

```
┌─────────────────────────────────────────────────────────────┐
│                     ReAct 执行流程                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   用户输入："帮我查北京天气并推荐景点"                         │
│                      ↓                                      │
│   ┌─────────────────────────────────────────┐               │
│   │ 循环 1                                   │               │
│   │  Thought: 需要先查询北京天气             │               │
│   │  Action: get_weather(city="北京")       │               │
│   │  Observation: 北京晴，15°C              │               │
│   └─────────────────────────────────────────┘               │
│                      ↓                                      │
│   ┌─────────────────────────────────────────┐               │
│   │ 循环 2                                   │               │
│   │  Thought: 根据晴天推荐景点               │               │
│   │  Action: get_attraction(...)            │               │
│   │  Observation: 推荐故宫、颐和园...        │               │
│   └─────────────────────────────────────────┘               │
│                      ↓                                      │
│   ┌─────────────────────────────────────────┐               │
│   │ 循环 3                                   │               │
│   │  Thought: 已有足够信息                   │               │
│   │  Action: Finish[最终答案]                │               │
│   └─────────────────────────────────────────┘               │
│                      ↓                                      │
│   输出最终答案                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 任务 4：修改问题观察变化

**你需要做的事情** 🧠：

修改 `FirstAgentTest.py` 中的问题，观察 Agent 行为变化：

```python
# 找到这行代码（约第157行）
user_prompt = "你好，请帮我查询一下今天北京的天气，然后根据天气推荐一个合适的旅游景点。"

# 修改为其他问题，例如：
user_prompt = "帮我查一下上海今天下雨吗？如果下雨推荐室内景点。"
# 或
user_prompt = "查询深圳的天气，然后推荐适合的美食。"
```

**观察要点**：

| 修改内容 | 观察重点 |
|----------|----------|
| 更换城市 | Agent 是否正确传递参数 |
| 更换需求 | Agent 是否调用不同工具 |
| 增加复杂度 | 循环次数是否增加 |

---

### 任务 5：尝试添加新工具

**你需要做的事情** 🧠：

在 `FirstAgentTest.py` 中添加一个简单的计算器工具：

```python
# 1. 定义工具函数（添加到 get_attraction 函数后面）
def calculate(expression: str) -> str:
    """
    计算数学表达式。
    支持加减乘除运算。
    """
    try:
        # 安全起见，只允许基本运算
        allowed_chars = set('0123456789+-*/(). ')
        if not all(c in allowed_chars for c in expression):
            return "错误：只支持基本数学运算（+-*/）"
        
        result = eval(expression)
        return f"计算结果：{expression} = {result}"
    except Exception as e:
        return f"计算错误：{e}"

# 2. 将工具添加到字典中
available_tools = {
    "get_weather": get_weather,
    "get_attraction": get_attraction,
    "calculate": calculate,  # 新增
}

# 3. 修改系统提示词，添加工具说明
AGENT_SYSTEM_PROMPT = """
你是一个智能旅行助手。你的任务是分析用户的请求，并使用可用工具一步步地解决问题。

# 可用工具:
- `get_weather(city: str)`: 查询指定城市的实时天气。
- `get_attraction(city: str, weather: str)`: 根据城市和天气搜索推荐的旅游景点。
- `calculate(expression: str)`: 计算数学表达式，如 "1+2*3"。

...（其余内容保持不变）
"""

# 4. 测试新工具
user_prompt = "帮我算一下，如果门票每人80元，我们5个人一共多少钱？"
```

**AI 可以帮助的事情** 🤖：

```
我想在 FirstAgentTest.py 中添加一个新工具 [工具名]，功能是 [功能描述]。
请帮我：
1. 编写工具函数代码
2. 说明如何注册到 available_tools
3. 如何更新系统提示词
4. 给出一个测试用例
```

---

### 任务 6：记录学习笔记

**你需要做的事情** 🧠：

创建学习笔记：

```markdown
# Day 3-4 学习笔记

## 代码执行流程理解

### FirstAgentTest.py 结构
1. 
2. 
3. 

### ReAct 循环
- Thought:
- Action:
- Observation:

## 实验记录

### 实验1：更换城市
- 问题：
- 结果：
- 观察：

### 实验2：添加计算器工具
- 代码：
- 测试：
- 结果：

## 遇到的问题
1. 
   - 解决方案：

## 心得体会
-

## 待解决问题
- [ ]
```

---

## 🤖 AI 辅助提示词库

### 代码理解

```
请解释以下代码段的作用：
[粘贴代码]

请说明：
1. 这段代码实现了什么功能？
2. 关键变量/参数的含义
3. 可能的边界情况
```

### 流程分析

```
请用流程图描述以下代码的执行流程：
[粘贴代码]

重点关注：
1. 循环条件和退出机制
2. 数据如何在各步骤间传递
3. 错误处理机制
```

### 工具开发

```
我想为 Agent 开发一个新工具，需求是：
[描述需求]

请帮我：
1. 设计工具接口（函数签名）
2. 编写实现代码
3. 编写使用示例
4. 说明注意事项
```

---

## 🧠 自主学习重点

### 必须自己做的事情

| 事项 | 原因 |
|------|------|
| 亲手运行代码 | 感受执行过程，发现细节 |
| 修改参数观察变化 | 理解参数对行为的影响 |
| 自己添加工具 | 掌握工具开发流程 |
| 画流程图 | 深刻理解执行逻辑 |

### 需要理解的概念

| 概念 | 理解程度要求 |
|------|-------------|
| 系统提示词 | 知道如何定义 Agent 行为 |
| 工具函数 | 知道如何编写和注册 |
| ReAct 循环 | 能描述每个步骤的作用 |
| 观察结果处理 | 理解如何传递给下一轮 |

---

## ✅ 完成标准

今天的学习完成后，你应该：

- [ ] 成功运行 FirstAgentTest.py
- [ ] 能用自己的话解释 ReAct 循环
- [ ] 能画出代码执行流程图
- [ ] 尝试修改问题并观察变化
- [ ] 成功添加至少一个新工具
- [ ] 学习笔记已记录

---

## 📚 扩展阅读

- `docs/chapter1/第一章 初识智能体.md`
- `docs/chapter4/第四章 智能体经典范式构建.md`

---

*[返回周计划](./plan.md)*
